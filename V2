local ScreenGui = Instance.new("ScreenGui")
local Frame = Instance.new("Frame")
local DupeButton = Instance.new("TextButton")
local ResetButton = Instance.new("TextButton")
local RejoinButton = Instance.new("TextButton")
jjj
-- Setting up the ScreenGui
ScreenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")

-- Setting up the Frame
Frame.Size = UDim2.new(0.3, 0, 0.4, 0)
Frame.Position = UDim2.new(0.35, 0, 0.3, 0)
Frame.BackgroundColor3 =dnjssjsjdjjfjfjjfj Color3.fromRGB(50, 50, 50)
Frame.Parent = ScreenGui

-- Setting up the Dupe Button
DupeButton.Size = UDim2.new(0.8, 0, 0.2, 0)
DupeButton.Position = UDim2.new(0.1, 0, 0.1, 0)
DupeButton.BackgroundColor3 = Color3.fromRGB(0, 0, 255)
DupeButton.Text = "DUPE"
DupeButton.Parent = Frame

-- Setting up the Reset Button
ResetButton.Size = UDim2.new(0.8, 0, 0.2, 0)
ResetButton.Position = UDim2.new(0.1, 0, 0.4, 0)
ResetButton.BackgroundColor3 = Color3.fromRGB(0, 0, 255)
ResetButton.Text = "RESET"
ResetButton.Parent = Frame

-- Setting up the Rejoin Button
RejoinButton.Size = UDim2.new(0.8, 0, 0.2, 0)
RejoinButton.Position = UDim2.new(0.1, 0, 0.7, 0)
RejoinButton.BackgroundColor3 = Color3.fromRGB(0, 0, 255)
RejoinButton.Text = "REJOIN SERVER"
RejoinButton.Parent = Frame

-- Button Functions
DupeButton.MouseButton1Click:Connect(function()
    print("Dupe function triggered!")
    -- Add your dupe logic here
end)

ResetButton.MouseButton1Click:Connect(function()
    print("Reset function triggered!")
    -- Add your reset logic here
end)

RejoinButton.MouseButton1Click:Connect(function()
    print("Rejoin function triggered!")
    -- Add your rejoin logic here
end)
